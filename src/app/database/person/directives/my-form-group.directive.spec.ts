import { MyFormGroupDirective } from './my-form-group.directive';

describe('MyFormGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new MyFormGroupDirective();
    expect(directive).toBeTruthy();
  });
});
