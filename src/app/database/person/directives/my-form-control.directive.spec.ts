import { MyFormControlDirective } from './my-form-control.directive';

describe('MyFormControlDirective', () => {
  it('should create an instance', () => {
    const directive = new MyFormControlDirective();
    expect(directive).toBeTruthy();
  });
});
