import { HelperContentDirective } from './helper-content.directive';

describe('HelperContentDirective', () => {
  it('should create an instance', () => {
    // const directive = new HelperContentDirective();
    expect(true).toBeTruthy();
  });
});
